# ===========================================================
#  .gitignore — Repo dashboard-de-lla (Tablero CBM / Data Engineering)
#  Solo se publica el tablero (index.html). Todo lo demas se ignora.
# ===========================================================

# Ignorar TODO por defecto...
*

# ...pero permitir explicitamente lo que SI se publica:
!.gitignore
!README.md
!index.html

# Nota: Dashboard_DE.html se copia a index.html al publicar,
# por eso NO se sube el original (solo el index.html resultante).
# Tampoco se suben los Excel (Base_DE), scripts (.py), .bat, ni backups.
